import hero from './hero';
import quote from './quote';
import aboutMe from './about-me';
import map from './map';

const homepage = {
  name: 'homepage',
  label: 'Головна',
  files: [
    hero,
    quote,
    aboutMe,
    map
  ]
};

export default homepage;
